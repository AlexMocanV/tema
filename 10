#include <iostream>

using namespace std;

int main()
{
    long long int a,b,i,s=0,c;
    cin>>a>>b;
    for(i=a;i<=b;i++)
    {
        s=0;
        c=i;
        while(c>0)
        {
            s+=c%10;
            c=c/10;
        }
        if(s%2==0)
        {
            cout<<"suma cifrelor e para\n";
        }
        else
        cout<<"suma cifrelor nu este para\n";
    }
    return 0;
}
