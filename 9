#include <iostream>

using namespace std;

int main()
{
    long long int n,uc,p=1,m=0,a,b,ogl,c,i,y;
    cin>>n;
    for(i=1;i<=n;i++)
    {
        cin>>y;
        c=y;
        ogl=0;
        while(c>0)
        {
            ogl=ogl*10+c%10;
            c=c/10;
        }
        if(ogl==y)
        {
            cout<<y<<" ";
        }
    }
    return 0;
}
